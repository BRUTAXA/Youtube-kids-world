# Max — Temporary Upload Folder

This old folder is kept temporarily because the image was uploaded here through the GitHub web UI.

The canonical Max Bible location is now:

```text
bible/characters/max/
```

Canonical files:

- `bible/characters/max/README.md`
- `bible/characters/max/STATUS.md`
- `decisions/DEC-Max-001.md`

Please move the image manually:

```text
characters/max/Max.png
→ bible/characters/max/max_master.png
```

After the image is moved, this temporary folder can be deleted.

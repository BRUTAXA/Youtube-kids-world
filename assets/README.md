# Shared Assets

Shared reusable production assets live here.

Examples:

- reusable props;
- backgrounds;
- sound library;
- music fragments;
- common visual elements.

If an asset belongs only to one episode, keep it inside that episode folder instead.
